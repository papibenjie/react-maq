import React from 'react';
import { NavItem } from '.'

export default {
  title: 'navigation/NavItem',
  component: NavItem,
  argTypes: {

  },
};

export const Basic = (args) => <NavItem {...args} />;