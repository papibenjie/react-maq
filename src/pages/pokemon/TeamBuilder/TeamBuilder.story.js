import React from 'react';
import { TeamBuilder } from '.'

export default {
  title: 'pokemon/TeamBuilder',
  component: TeamBuilder,
  argTypes: {

  },
};

export const Basic = (args) => <TeamBuilder {...args} />;